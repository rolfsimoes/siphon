% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-parallel.R
\name{parallel_setup_workers}
\alias{parallel_setup_workers}
\title{Run setup code on all workers of a parallel backend}
\usage{
parallel_setup_workers(backend, expr)
}
\arguments{
\item{backend}{A backend object created by \code{\link[=parallel_backend]{parallel_backend()}}.}

\item{expr}{An expression to evaluate on each worker.}
}
\value{
The backend, invisibly.
}
\description{
\code{parallel_setup_workers()} evaluates an expression in the global
environment of every worker of a parallel backend. Use it to load
packages, source files, or define objects that jobs need. The expression
is recorded and replayed automatically on any replacement node created
after a worker failure. It may be called before the backend's cluster
exists: the expression is queued and replayed when the backend opens.
}
\details{
The expression is captured unevaluated. Values from the calling
frame can be injected by wrapping them in double braces, e.g.
\code{parallel_setup_workers(bk, x <- {{ y }})} assigns the current value of
\code{y} to \code{x} on each worker.

Setup can only run while no jobs are active on the backend.
}
\examples{
if (requireNamespace("parallel", quietly = TRUE)) {
    bk <- parallel_backend(2)
    parallel_setup_workers(bk, threshold <- 10)
    f <- 1:5 |>
        pump(function(x) x + threshold, backend = bk)
    pump_run(f, verbose = FALSE)
    parallel_stop(bk)
}
}
\seealso{
\code{\link[=parallel_backend]{parallel_backend()}}, \code{\link[=parallel_stop]{parallel_stop()}}
}
