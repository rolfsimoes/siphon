% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pump.R
\name{pump}
\alias{pump}
\title{Add a processing stage to a pipeline}
\usage{
pump(
  x,
  fn,
  ...,
  backend = NULL,
  max_workers = NULL,
  on_error = NULL,
  buffer_size = NULL
)
}
\arguments{
\item{x}{A pump object or a finite R object (list, vector) that will be
implicitly wrapped as a basic source.}

\item{fn}{A function. It receives one item as its first argument.}

\item{...}{Additional arguments passed to \code{fn}.}

\item{backend}{A backend object or one of \code{"main"}, \code{"mirai"},
or \code{"future"}. Use \code{parallel_backend()} directly for fault-tolerant
PSOCK execution (no string alias). If \code{NULL} (the default), the stage
inherits the backend set by \code{pump_run()} or \code{pump_drain()}.}

\item{max_workers}{Maximum number of active jobs for this stage. Defaults to the
backend worker count. Ignored for the synchronous \code{main_backend()} (which always uses 1).}

\item{on_error}{How to handle item errors: \code{"stop"} throws on first error,
\code{"collect"} propagates them, \code{"continue"} drops failed items. If \code{NULL}
(the default), the stage inherits the policy set by \code{pump_run()}.}

\item{buffer_size}{Maximum size of the output buffer. Defaults to
\code{min(length(x), 1000L)}. Use a smaller value to enable true backpressure.}
}
\value{
A pump object.
}
\description{
\code{pump()} creates a pull-based stage. The stage pulls items from its upstream
source when it has free slots, submits jobs to the selected backend, and
yields completed items to downstream stages.
}
\details{
Stages implement a two-phase protocol: \verb{$next_item()} runs one \emph{beat}
(harvest completed jobs into the output buffer, then refill free slots by
pulling upstream — safe to call repeatedly) and \verb{$pop_item()} consumes one
ready result. For interactive use prefer the exported verbs \code{\link[=pump_step]{pump_step()}},
\code{\link[=pump_peek]{pump_peek()}}, and \code{\link[=pump_pop]{pump_pop()}}; inspect state with \code{print()} or
\code{\link[=pump_status]{pump_status()}}.
}
\examples{
# Single stage pipeline
f <- 1:5 |> pump(function(x) x * 2, backend = "main")
pump_run(f, verbose = FALSE)

# Two-stage pipeline
f <- 1:5 |>
    pump(function(x) x + 1, backend = "main") |>
    pump(function(x) x * 3, backend = "main")
pump_run(f, verbose = FALSE)
}
