% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{pump_peek}
\alias{pump_peek}
\title{Peek at ready results without consuming them}
\usage{
pump_peek(x, n = 1L)
}
\arguments{
\item{x}{A pump object.}

\item{n}{Maximum number of items to return. Defaults to 1.}
}
\value{
A list of up to \code{n} ready items (empty when nothing is ready).
Sources have no buffer, so peeking a bare source returns an empty list.
}
\description{
\code{pump_peek()} returns up to \code{n} items that are ready in the terminal
stage's output buffer, without removing them: calling it repeatedly
returns the same items, and the pipeline is unaffected.
}
\details{
Each item is a list with \code{id}, \code{idx}, \code{data}, and \code{ok}. Use \code{\link[=pump_step]{pump_step()}}
first to advance work into the buffer.
}
\examples{
p <- 1:5 |> pump(function(x) x * 2, backend = "main")
pump_step(p)
pump_peek(p)
}
\seealso{
Other inspection:
\code{\link[=pump_pop]{pump_pop()}},
\code{\link[=pump_step]{pump_step()}}
}
\concept{inspection}
