% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source.R
\name{pump_source}
\alias{pump_source}
\title{Create a custom siphon source}
\usage{
pump_source(pull_fn, done_fn = NULL, close_fn = NULL, length = Inf)
}
\arguments{
\item{pull_fn}{A function with no arguments that returns
\code{list(id, data, ok)} or \code{NULL}. The returned list must contain a
non-missing, scalar atomic user-visible \code{id} (uniqueness is the user's
responsibility), the \code{data} object, and a non-missing scalar logical \code{ok}
flag indicating success.}

\item{done_fn}{A function with no arguments returning \code{TRUE} or \code{FALSE}.
Defaults to \code{NULL} (source never finishes on its own).}

\item{close_fn}{An optional function with no arguments for resource cleanup
(e.g., closing file connections or database handles). Called automatically
by \code{pump_run()} and \code{pump_drain()} when execution completes.}

\item{length}{The total number of items to expect. Defaults to \code{Inf}.
Used by \code{pump_run()} for result pre-allocation and progress reporting.}
}
\value{
A pump object that can be piped into \code{pump()}.
}
\description{
\code{pump_source()} creates a custom pull-based source for use with \code{pump()}
pipelines. Use this to connect external data sources such as message queues,
databases, or file readers to a siphon pipeline.
}
\details{
The \code{pull_fn} function is called repeatedly by downstream stages to retrieve
items. It should return \code{list(id, data, ok)} when an item is available, or
\code{NULL} when no item is ready. The source is considered infinite by default
(suitable for daemon-style processing); pass a \code{done_fn} to signal when the
source is exhausted.
}
\examples{
# A simple counter source
counter_source <- function(n) {
    i <- 0L
    pump_source(
        pull_fn = function() {
            if (i >= n) {
                return(NULL)
            }
            i <<- i + 1L
            list(id = i, data = i, ok = TRUE)
        },
        done_fn = function() i >= n,
        length = n
    )
}
src <- counter_source(5)
res <- src |>
    pump(function(x) x * 2) |>
    pump_run(verbose = FALSE)
print(res)
}
