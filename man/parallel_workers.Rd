% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-parallel.R
\name{parallel_workers}
\alias{parallel_workers}
\alias{parallel_busy}
\title{Inspect the workers of a parallel backend}
\usage{
parallel_workers(backend)

parallel_busy(backend)
}
\arguments{
\item{backend}{A backend object created by \code{\link[=parallel_backend]{parallel_backend()}}.}
}
\value{
\code{parallel_workers()}: an integer count. \code{parallel_busy()}: a
logical vector with one element per live worker.
}
\description{
\code{parallel_workers()} returns the number of worker processes of a
parallel backend. \code{parallel_busy()} returns a logical vector with one
element per worker: \code{TRUE} for workers currently holding an in-flight
job. Owners of attached clusters (see \code{parallel_backend(cluster =)})
can use \code{parallel_busy()} after a failed run to find nodes that were
quarantined (left busy) and repair them before reusing the cluster
elsewhere.
}
\details{
A backend created with \code{workers} is a specification until its
first use: \code{parallel_workers()} then reports the configured capacity
and \code{parallel_busy()} returns a zero-length vector. On a stopped
backend, \code{parallel_workers()} returns 0 and \code{parallel_busy()} a
zero-length vector.
}
\examples{
if (requireNamespace("parallel", quietly = TRUE)) {
    bk <- parallel_backend(2)
    parallel_workers(bk)
    parallel_busy(bk)
    parallel_stop(bk)
}
}
\seealso{
\code{\link[=parallel_backend]{parallel_backend()}}, \code{\link[=parallel_stop]{parallel_stop()}}
}
