% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-parallel.R
\name{parallel_stop}
\alias{parallel_stop}
\title{Stop a parallel backend}
\usage{
parallel_stop(backend, force = FALSE)
}
\arguments{
\item{backend}{A backend object created by \code{\link[=parallel_backend]{parallel_backend()}}.}

\item{force}{If \code{TRUE}, stop the cluster even if jobs are active.}
}
\value{
The backend, invisibly.
}
\description{
\code{parallel_stop()} shuts down the PSOCK cluster owned by a parallel
backend. Call it when the backend is no longer needed to release the
worker processes and their socket connections.
}
\details{
By default, stopping fails if jobs are still active. Set
\code{force = TRUE} to stop the cluster regardless. Stopping an already
stopped backend is a no-op.
}
\examples{
if (requireNamespace("parallel", quietly = TRUE)) {
    bk <- parallel_backend(2)
    parallel_stop(bk)
}
}
\seealso{
\code{\link[=parallel_backend]{parallel_backend()}}
}
