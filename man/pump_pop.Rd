% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{pump_pop}
\alias{pump_pop}
\title{Consume one ready result from a pipeline}
\usage{
pump_pop(x)
}
\arguments{
\item{x}{A pump object.}
}
\value{
A list with \code{id}, \code{idx}, \code{data}, and \code{ok}, or \code{NULL} when no item
is ready.
}
\description{
\code{pump_pop()} removes and returns the next ready item from the terminal
stage's output buffer, or \code{NULL} when nothing is ready. \strong{This consumes
the item}: it will not appear in the results of a later \code{pump_run()},
and responsibility for its lifecycle transfers to you. For managed
sources (see \code{\link[=pump_source]{pump_source()}}), call \code{x$item_commit(id, data)} and
\code{x$item_release(id)} after successfully handling the item, exactly as
\code{pump_run()} does; use \code{\link[=pump_peek]{pump_peek()}} instead when you only want to look.
}
\examples{
p <- 1:5 |> pump(function(x) x * 2, backend = "main")
pump_step(p)
v <- pump_pop(p)
v$data
}
\seealso{
Other inspection:
\code{\link[=pump_peek]{pump_peek()}},
\code{\link[=pump_step]{pump_step()}}
}
\concept{inspection}
