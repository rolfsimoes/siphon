% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-mirai.R
\name{mirai_backend}
\alias{mirai_backend}
\title{Create a mirai backend}
\usage{
mirai_backend()
}
\value{
A backend object.
}
\description{
\code{mirai_backend()} submits jobs through \code{mirai::mirai()}. The number of
available slots is read from \code{mirai::status()$connections}.
}
\details{
Note: When using mirai_backend(), you are responsible for managing
the mirai daemon lifecycle. Call \code{mirai::daemons(n)} to start workers and
\code{mirai::daemons(0)} to shut them down. See the vignette for examples.
}
\examples{
if (requireNamespace("mirai", quietly = TRUE) &&
    mirai::status()$connections > 0) {
    f <- 1:5 |>
        pump(function(x) x * 2, backend = mirai_backend())
    pump_run(f, verbose = FALSE)
}
}
