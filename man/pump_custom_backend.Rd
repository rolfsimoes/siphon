% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-custom.R
\name{pump_custom_backend}
\alias{pump_custom_backend}
\title{Create a custom backend from plain functions}
\usage{
pump_custom_backend(
  name,
  count,
  submit,
  is_ready,
  collect,
  register = NULL,
  open = NULL
)
}
\arguments{
\item{name}{A single string identifying the backend in \code{print()} and
\code{\link[=pump_status]{pump_status()}} output.}

\item{count}{A function with no arguments returning the number of
concurrently runnable jobs.}

\item{submit}{A function \verb{function(handle, data)} starting one job and
returning a job token.}

\item{is_ready}{A function \verb{function(token)} returning \code{TRUE} when the
job's result can be collected without blocking.}

\item{collect}{A function \verb{function(token)} returning the finished value
(or a condition object to mark the item as failed).}

\item{register}{An optional function \verb{function(func, args)} returning
the stage handle passed to \code{submit()}. Defaults to bundling \code{func} and
\code{args} in a list.}

\item{open}{An optional function with no arguments, called once before
the backend is first used.}
}
\value{
A backend object usable as the \code{backend} argument of \code{\link[=pump]{pump()}},
\code{\link[=pump_run]{pump_run()}}, and \code{\link[=pump_drain]{pump_drain()}}.
}
\description{
\code{pump_custom_backend()} builds a siphon backend from a handful of plain R
functions, so tools with their own execution machinery (job queues,
remote services, worker pools) can plug into \code{pump()} pipelines without
touching siphon internals.
}
\details{
siphon drives every backend through the same small contract. You supply
each operation as a function:
\itemize{
\item \code{count()} is consulted when stages are sized and validated: it must
return the number of jobs the backend can run concurrently, at least 1.
\item \code{register(func, args)} is called once per stage, after \code{open()}. It
receives the stage function and its constant extra arguments, and
returns an opaque \emph{handle} that later submissions can use. This is the
place to install per-stage state on your workers so each job ships
only its item data. When omitted, the handle is
\verb{list(func = <stage function>, args = <constant arguments>)}.
\item \code{submit(handle, data)} starts one job for one item and returns a job
\emph{token} without blocking. The job must compute
\verb{func(data, ...constant args...)} - with the default handle,
\code{do.call(handle$func, c(list(data), handle$args))}.
\item \code{is_ready(token)} is polled between beats and must return \code{TRUE} once
\code{collect()} will not block. It must never block itself.
\item \code{collect(token)} returns the finished job's value. Return a condition
object (e.g. from \code{tryCatch(..., error = identity)}) to mark the item
as \emph{failed}: the pipeline then applies the stage's \code{on_error} policy.
An error \emph{thrown} by \code{collect()} itself is treated as an
infrastructure failure and aborts the pipeline. Note that a condition
returned this way is always interpreted as failure - if your items can
legitimately be condition objects, wrap them in a list.
\item \code{open()} (optional) is called once before the backend is first used -
the place for lazy resource acquisition. siphon guarantees at most one
\code{open()} call per backend object.
}

Resource lifecycle stays yours, as with \code{\link[=mirai_backend]{mirai_backend()}} daemons and
\code{\link[=future_backend]{future_backend()}} plans: shut down whatever \code{open()} or your
constructor started when you no longer need the backend.

Per-item function timing (\code{fn_time} in \code{\link[=pump_status]{pump_status()}}) is not measured
for custom backends and reports as zero; scheduling and dispatch timings
are reported as usual.
}
\examples{
# A toy synchronous backend: runs each job at submit time
toy_backend <- pump_custom_backend(
    name = "toy",
    count = function() 1L,
    submit = function(handle, data) {
        tryCatch(
            do.call(handle$func, c(list(data), handle$args)),
            error = identity
        )
    },
    is_ready = function(token) TRUE,
    collect = function(token) token
)
f <- 1:5 |> pump(function(x) x * 2, backend = toy_backend)
pump_run(f, verbose = FALSE)
}
\seealso{
\code{vignette("extending", package = "siphon")} for a walkthrough,
including an asynchronous example.
}
